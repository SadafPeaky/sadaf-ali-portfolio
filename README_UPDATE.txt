V8 EDUCATION FIX — SAFE UPDATE

IMPORTANT:
This update intentionally does NOT include config.js or schema.sql.

Use this ZIP by extracting/copying its files INTO the existing portfolio folder that already contains your working config.js.
Do NOT delete or replace your existing config.js.

The update fixes:
- Education Save button
- Education fields no longer disappear when clicking + Add education
- Remove preserves unsaved education values
- Education changes can be saved to Supabase

Your existing Supabase connection remains untouched.
